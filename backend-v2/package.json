{
  "scripts": {
    "prebuild": "npm run migration:run || echo 'Migration skipped'",
    "build": "nest build || (echo 'Build failed, checking issues...' && exit 1)",
    "postbuild": "node copy-views.js || echo 'copy-views skipped'",
    "start": "node dist/main",
    "start:dev": "nest start --watch"
  }
}
