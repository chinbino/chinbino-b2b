{
  "scripts": {
    "build": "nest build",
    "start": "nest start --port $PORT",
    "start:dev": "nest start --watch",
    "start:prod": "nest start --port $PORT",
    "migration:run": "echo 'Migrations temporarily disabled' && exit 0",
    "migration:generate": "echo 'Migrations temporarily disabled'"
  }
}
