<!DOCTYPE html>
<html dir="rtl" lang="fa">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}} - ChinBino Admin</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.1/font/bootstrap-icons.css">
    <style>
        :root {
            --primary-color: #2c3e50;
            --secondary-color: #3498db;
            --success-color: #27ae60;
            --danger-color: #e74c3c;
        }
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f8f9fa;
        }
        .admin-sidebar {
            background: var(--primary-color);
            color: white;
            min-height: 100vh;
            position: fixed;
            width: 250px;
        }
        .admin-content {
            margin-right: 250px;
            padding: 20px;
        }
        .sidebar-brand {
            padding: 20px;
            border-bottom: 1px solid rgba(255,255,255,0.1);
        }
        .sidebar-menu {
            list-style: none;
            padding: 0;
        }
        .sidebar-menu li a {
            color: rgba(255,255,255,0.8);
            display: block;
            padding: 12px 20px;
            text-decoration: none;
            transition: all 0.3s;
        }
        .sidebar-menu li a:hover {
            background: rgba(255,255,255,0.1);
            color: white;
        }
        .sidebar-menu li.active a {
            background: var(--secondary-color);
            color: white;
        }
        .admin-header {
            background: white;
            border-bottom: 1px solid #dee2e6;
            padding: 15px 0;
            margin-bottom: 20px;
        }
        .card {
            border: none;
            box-shadow: 0 0.125rem 0.25rem rgba(0, 0, 0, 0.075);
        }
        .status-badge {
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 0.85rem;
        }
        .status-active {
            background-color: rgba(40, 167, 69, 0.1);
            color: #28a745;
        }
        .status-inactive {
            background-color: rgba(220, 53, 69, 0.1);
            color: #dc3545;
        }
    </style>
</head>
<body>
    <div class="admin-sidebar">
        <div class="sidebar-brand">
            <h4><i class="bi bi-shop"></i> ChinBino Admin</h4>
            <small class="text-muted">B2B Backend V2</small>
        </div>
        <ul class="sidebar-menu">
            <li class="active">
                <a href="/admin/sellers">
                    <i class="bi bi-people"></i> مدیریت فروشندگان
                </a>
            </li>
            <li>
                <a href="#">
                    <i class="bi bi-box"></i> محصولات
                </a>
            </li>
            <li>
                <a href="#">
                    <i class="bi bi-cart"></i> سفارشات
                </a>
            </li>
            <li>
                <a href="#">
                    <i class="bi bi-bar-chart"></i> آمار
                </a>
            </li>
            <li>
                <a href="#">
                    <i class="bi bi-gear"></i> تنظیمات
                </a>
            </li>
        </ul>
    </div>

    <div class="admin-content">
        <div class="admin-header">
            <div class="container-fluid">
                <div class="d-flex justify-content-between align-items-center">
                    <h2 class="mb-0">{{title}}</h2>
                    <div class="text-muted">
                        <small>آخرین بروزرسانی: {{currentDate}}</small>
                    </div>
                </div>
            </div>
        </div>

        <div class="container-fluid">
            {{{body}}}
        </div>

        <footer class="mt-5 pt-3 border-top text-center text-muted">
            <p>ChinBino B2B Backend V2 &copy; 2023 | توسعه‌یافته با NestJS</p>
            <small>Version: Phase C2.1</small>
        </footer>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // تاریخ شمسی
        const persianDate = new Date().toLocaleDateString('fa-IR');
        document.querySelectorAll('.current-date').forEach(el => {
            el.textContent = persianDate;
        });
    </script>
</body>
</html>
