<!DOCTYPE html>
<html dir="rtl" lang="fa">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}} - ChinBino Admin</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { font-family: Tahoma; background-color: #f8f9fa; padding: 20px; }
        .container { max-width: 800px; margin: 0 auto; background: white; padding: 30px; border-radius: 10px; }
        h1 { color: #2c3e50; }
    </style>
</head>
<body>
    <div class="container">
        <h1>{{title}}</h1>
        <p>تاریخ: {{currentDate}}</p>
        <hr>
        
        <div class="card">
            <div class="card-body">
                <h4>ویرایش فروشنده #{{seller.id}}</h4>
                <p class="text-muted">نام: {{seller.name}}</p>
                
                <div class="mb-3">
                    <label class="form-label">وضعیت حساب:</label>
                    <select class="form-select">
                        <option value="active" {{#if (eq seller.status "active")}}selected{{/if}}>فعال</option>
                        <option value="inactive" {{#if (eq seller.status "inactive")}}selected{{/if}}>غیرفعال</option>
                    </select>
                </div>
                
                <div class="mt-3">
                    <button class="btn btn-primary">ذخیره تغییرات</button>
                    <a href="/admin/sellers" class="btn btn-secondary">بازگشت</a>
                </div>
            </div>
        </div>
        
        <div class="mt-4 text-center text-muted">
            <p>✅ Phase C2.1: Viewهای Handlebars کامل شدند</p>
        </div>
    </div>
</body>
</html>
