<!DOCTYPE html>
<html dir="rtl" lang="fa">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}} - ChinBino</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-4">
        <h1>{{title}}</h1>
        <p>تاریخ: {{currentDate}}</p>
        <hr>
        
        <div class="card">
            <div class="card-body">
                <h4>ویرایش فروشنده #{{seller.id}}</h4>
                <p class="text-muted">نام: {{seller.name}}</p>
                
                <form>
                    <div class="mb-3">
                        <label class="form-label">نام فروشنده:</label>
                        <input type="text" class="form-control" value="{{seller.name}}">
                    </div>
                    
                    <div class="mb-3">
                        <label class="form-label">ایمیل:</label>
                        <input type="email" class="form-control" value="{{seller.email}}">
                    </div>
                    
                    <div class="mb-3">
                        <label class="form-label">وضعیت:</label>
                        <select class="form-select">
                            <option value="active" {{#if (eq seller.status 'active')}}selected{{/if}}>فعال</option>
                            <option value="inactive" {{#if (eq seller.status 'inactive')}}selected{{/if}}>غیرفعال</option>
                        </select>
                    </div>
                    
                    <button type="submit" class="btn btn-primary">بروزرسانی</button>
                    <a href="/admin/sellers" class="btn btn-secondary">بازگشت</a>
                </form>
            </div>
        </div>
        
        <div class="mt-4 text-center text-muted">
            <p>🎯 Phase C2.1: Viewهای Handlebars با ساختار استاندارد پیاده‌سازی شد</p>
        </div>
    </div>
</body>
</html>
