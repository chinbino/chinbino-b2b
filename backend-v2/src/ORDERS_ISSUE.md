# 🚨 مشکل ماژول Orders - نیاز به Refactor

## وضعیت فعلی:
- ❌ تداخل Entityها با CMS
- ❌ خطاهای TypeORM در روابط
- ❌ Migrationهای ناسازگار

## ریشه مشکلات:
1. **تداخل روابط:** `OrderItem` به `SellerOrder` و `OrderItemAddon` اشاره می‌کند
2. **Missing Entities:** برخی Entityها در TypeORM register نشده‌اند
3. **Migration Conflicts:** Foreign keyهای ناسازگار

## راه‌حل پیشنهادی:
1. **بازنویسی کامل ماژول Orders** با معماری جدید
2. **استفاده از Transaction Pattern** برای مدیریت سفارشات
3. **جداسازی کامل از CMS**

## زمان‌بندی بازگشت:
- فاز ۱: پایدارسازی CMS (الان)
- فاز ۲: طراحی معماری جدید Orders (۲-۳ روز)
- فاز ۳: پیاده‌سازی و تست (۳-۴ روز)
