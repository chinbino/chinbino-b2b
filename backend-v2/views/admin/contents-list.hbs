{{!< layouts/admin}}

{{#content "title"}}لیست محتواها{{/content}}

<div class="mb-6 flex justify-between items-center">
    <h2 class="text-2xl font-bold text-gray-800">مدیریت محتواها</h2>
    <a href="/admin/contents/new" class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-lg transition">
        ➕ محتوای جدید
    </a>
</div>

<!-- فیلترها -->
<div class="bg-white p-4 rounded-lg shadow mb-6">
    <form method="GET" class="flex gap-4">
        <div>
            <label class="block text-sm font-medium text-gray-700 mb-1">نوع محتوا</label>
            <select name="type" class="border border-gray-300 rounded px-3 py-2 w-40">
                <option value="">همه</option>
                <option value="article" {{#eq filters.type "article"}}selected{{/eq}}>مقاله</option>
                <option value="landing_market" {{#eq filters.type "landing_market"}}selected{{/eq}}>صفحه بازار</option>
                <option value="static_page" {{#eq filters.type "static_page"}}selected{{/eq}}>صفحه ثابت</option>
            </select>
        </div>
        <div>
            <label class="block text-sm font-medium text-gray-700 mb-1">وضعیت</label>
            <select name="status" class="border border-gray-300 rounded px-3 py-2 w-40">
                <option value="">همه</option>
                <option value="draft" {{#eq filters.status "draft"}}selected{{/eq}}>پیش‌نویس</option>
                <option value="published" {{#eq filters.status "published"}}selected{{/eq}}>منتشر شده</option>
                <option value="archived" {{#eq filters.status "archived"}}selected{{/eq}}>آرشیو</option>
            </select>
        </div>
        <div class="flex items-end">
            <button type="submit" class="bg-gray-600 hover:bg-gray-700 text-white px-4 py-2 rounded">
                🔍 فیلتر
            </button>
            <a href="/admin/contents" class="bg-gray-300 hover:bg-gray-400 text-gray-700 px-4 py-2 rounded mr-2">
                پاک کردن
            </a>
        </div>
    </form>
</div>

<!-- جدول محتواها -->
<div class="bg-white rounded-lg shadow overflow-hidden">
    <table class="min-w-full divide-y divide-gray-200">
        <thead class="bg-gray-50">
            <tr>
                <th class="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase tracking-wider">ID</th>
                <th class="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase tracking-wider">نوع</th>
                <th class="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase tracking-wider">Slug</th>
                <th class="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase tracking-wider">عنوان</th>
                <th class="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase tracking-wider">وضعیت</th>
                <th class="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase tracking-wider">آخرین تغییر</th>
                <th class="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase tracking-wider">عملیات</th>
            </tr>
        </thead>
        <tbody class="bg-white divide-y divide-gray-200">
            {{#each contents}}
            <tr class="hover:bg-gray-50">
                <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">{{this.id}}</td>
                <td class="px-6 py-4 whitespace-nowrap">
                    <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-blue-100 text-blue-800">
                        {{this.type}}
                    </span>
                </td>
                <td class="px-6 py-4 whitespace-nowrap text-sm font-mono text-gray-600">{{this.slug}}</td>
                <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">{{this.title.fa}}</td>
                <td class="px-6 py-4 whitespace-nowrap">
                    {{#eq this.status "published"}}
                    <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800">
                        منتشر شده
                    </span>
                    {{/eq}}
                    {{#eq this.status "draft"}}
                    <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-yellow-100 text-yellow-800">
                        پیش‌نویس
                    </span>
                    {{/eq}}
                    {{#eq this.status "archived"}}
                    <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-red-100 text-red-800">
                        آرشیو
                    </span>
                    {{/eq}}
                </td>
                <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                    {{formatDate this.updatedAt}}
                </td>
                <td class="px-6 py-4 whitespace-nowrap text-sm font-medium">
                    <div class="flex gap-2">
                        <a href="/admin/contents/{{this.id}}/edit" class="text-blue-600 hover:text-blue-900">
                            ✏️ ویرایش
                        </a>
                        <a href="/admin/contents/{{this.id}}/preview" target="_blank" class="text-green-600 hover:text-green-900">
                            👁️ پیش‌نمایش
                        </a>
                        <a href="/admin/contents/{{this.id}}/json" target="_blank" class="text-gray-600 hover:text-gray-900">
                            🔍 JSON
                        </a>
                    </div>
                </td>
            </tr>
            {{/each}}
        </tbody>
    </table>

    {{#unless contents.length}}
    <div class="text-center py-8 text-gray-500">
        هیچ محتوایی یافت نشد
    </div>
    {{/unless}}
</div>
