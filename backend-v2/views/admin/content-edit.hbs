{{!< layouts/admin}}

{{#content "title"}}ویرایش محتوا{{/content}}

<div class="max-w-4xl mx-auto">
    <div class="mb-6">
        <h2 class="text-2xl font-bold text-gray-800">ویرایش محتوا</h2>
        <p class="text-gray-600 mt-1">شناسه: {{content.id}} | Slug: {{content.slug}}</p>
    </div>

    <form method="POST" action="/admin/contents/{{content.id}}/edit" class="bg-white p-6 rounded-lg shadow space-y-6">
        
        <!-- اطلاعات اصلی -->
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <div>
                <label class="block text-sm font-medium text-gray-700 mb-1">نوع محتوا *</label>
                <select name="type" required class="w-full border border-gray-300 rounded px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500">
                    {{#each contentTypes}}
                    <option value="{{this}}" {{#eq this ../content.type}}selected{{/eq}}>{{this}}</option>
                    {{/each}}
                </select>
            </div>
            
            <div>
                <label class="block text-sm font-medium text-gray-700 mb-1">Slug *</label>
                <input type="text" name="slug" required value="{{content.slug}}"
                    class="w-full border border-gray-300 rounded px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500">
            </div>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <div>
                <label class="block text-sm font-medium text-gray-700 mb-1">عنوان فارسی *</label>
                <input type="text" name="title_fa" required value="{{content.title.fa}}"
                    class="w-full border border-gray-300 rounded px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500">
            </div>
            
            <div>
                <label class="block text-sm font-medium text-gray-700 mb-1">وضعیت</label>
                <select name="status" class="w-full border border-gray-300 rounded px-3 py-2">
                    {{#each statusTypes}}
                    <option value="{{this}}" {{#eq this ../content.status}}selected{{/eq}}>{{this}}</option>
                    {{/each}}
                </select>
            </div>
        </div>

        <!-- خلاصه -->
        <div>
            <label class="block text-sm font-medium text-gray-700 mb-1">خلاصه فارسی</label>
            <textarea name="excerpt_fa" rows="3" 
                class="w-full border border-gray-300 rounded px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500">{{content.excerpt.fa}}</textarea>
        </div>

        <!-- سئو -->
        <div class="border-t pt-6">
            <h3 class="text-lg font-medium text-gray-900 mb-4">تنظیمات سئو</h3>
            
            <div class="space-y-4">
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-1">عنوان متا (فارسی)</label>
                    <input type="text" name="meta_title_fa" value="{{content.seo.metaTitle.fa}}"
                        class="w-full border border-gray-300 rounded px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500">
                </div>
                
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-1">توضیحات متا (فارسی)</label>
                    <textarea name="meta_description_fa" rows="2" 
                        class="w-full border border-gray-300 rounded px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500">{{content.seo.metaDescription.fa}}</textarea>
                </div>
            </div>
        </div>

        <!-- بلوک‌ها -->
        <div class="border-t pt-6">
            <h3 class="text-lg font-medium text-gray-900 mb-4">بلوک‌های محتوا</h3>
            <div class="bg-yellow-50 border border-yellow-200 rounded p-4 mb-4">
                <p class="text-yellow-800 text-sm">
                    💡 در این نسخه، بلوک‌ها به صورت JSON وارد می‌شوند. نسخه بعدی شامل ادیتور بصری خواهد بود.
                </p>
            </div>
            
            <label class="block text-sm font-medium text-gray-700 mb-1">JSON بلوک‌ها</label>
            <textarea name="blocks" rows="12" 
                class="w-full border border-gray-300 rounded px-3 py-2 font-mono text-sm focus:outline-none focus:ring-2 focus:ring-blue-500">{{json content.blocks}}</textarea>
        </div>

        <!-- دکمه‌ها -->
        <div class="flex gap-3 pt-6 border-t">
            <button type="submit" class="bg-blue-600 hover:bg-blue-700 text-white px-6 py-2 rounded-lg transition">
                💾 ذخیره تغییرات
            </button>
            <a href="/admin/contents/{{content.id}}/preview" target="_blank" class="bg-green-600 hover:bg-green-700 text-white px-6 py-2 rounded-lg transition">
                👁️ پیش‌نمایش
            </a>
            <a href="/admin/contents" class="bg-gray-300 hover:bg-gray-400 text-gray-700 px-6 py-2 rounded-lg transition">
                ❌ انصراف
            </a>
        </div>
    </form>
</div>
