# Chinbino CMS – Admin UI Minimal Spec (Phase A)

این سند مرجع رسمی برای پیاده‌سازی Admin UI مینیمال CMS در پروژه Chinbino است.  
هر تغییری در Admin UI باید بر اساس این سند انجام شود و این سند در طول کار به‌روزرسانی شود.

---

## 0. Environment & Constraints

- [x] **E0.1** – Backend روی Render دیپلوی شده است:
  - API Base URL (dev/staging):  
    `https://chinbino-api-v2.onrender.com`
- [x] **E0.2** – سورس‌کد روی GitHub مدیریت می‌شود.
- [x] **E0.3** – فعلاً محیط لوکال برای صاحب پروژه وجود ندارد و تست فقط از طریق:
  - Render logs
  - GitHub commits
  - HTTP requests به API انجام می‌شود.
- [x] **E0.4** – هر جایی که در گفتگو پیشنهاد "run locally / test locally" مطرح شود، باید نادیده گرفته شده و راه‌حل مناسب برای Render/GitHub ارائه شود.

---

## 1. وضعیت فعلی CMS (Backend)

### 1.1. Core CMS Features

- [x] **C1.1** – Entityهای اصلی CMS پیاده‌سازی شده‌اند:
  - `Content`
  - `ContentRevision`
  - `Block` / `blocks` (JSON)
  - `Asset`
- [x] **C1.2** – APIهای اصلی محتوا در دسترس هستند:
  - `POST /api/contents` – ایجاد محتوا
  - `GET /api/contents` – لیست محتوا
  - `GET /api/contents/:slug` – گرفتن محتوا بر اساس slug
  - `PUT /api/contents/:id` – آپدیت محتوا
- [x] **C1.3** – سیستم چندزبانه (fa/en) در محتوا و SEO فعال است.
- [x] **C1.4** – سیستم Revision برای محتوا فعال است.
- [x] **C1.5** – محتوای واقعی برای صفحه **بازار فوتین / Yiwu Market** ایجاد و تست شده است.

---

## 2. هدف Phase A – Admin UI Minimal

🎯 این فاز یک پنل مدیریت مینیمال، اما قابل استفاده برای CMS می‌سازد.

Base URL Admin:

- `https://chinbino-api-v2.onrender.com/admin`

Admin UI فعلاً:

- ساده است
- روی HTML server-side پیاده شده
- مستقیماً به دیتابیس CMS متصل است (بدون mock / فایل متنی)

---

## 3. Scope فاز A – لیست، ایجاد، ویرایش، پیش‌نمایش

### 3.1. Admin Module در NestJS (A3.1.x)

- [x] **A3.1.1** – ایجاد `AdminModule`:

ساختار:

```text
src/admin/
  ├── admin.module.ts
  ├── controllers/
  │   └── admin-contents.controller.ts
  ├── views/
  │   ├── layout.hbs
  │   ├── contents-list.hbs
  │   ├── content-edit.hbs
  │   └── content-create.hbs
  └── dto/
      └── admin-content.dto.ts
